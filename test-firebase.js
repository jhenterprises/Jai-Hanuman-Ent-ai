import { initializeApp } from "firebase/app";
import { getFirestore, doc, getDoc, setDoc } from "firebase/firestore";

const firebaseConfig = {
  // we will check if the user pancardjhc2018@gmail.com can be found
};
