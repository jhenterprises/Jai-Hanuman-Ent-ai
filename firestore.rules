rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Global permission rule to ensure smooth operations across all services, notifications, logs, and user sessions
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

